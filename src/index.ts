import { config, loadAndParse, loadAndRender, parse, render } from './atat';
import { __express } from './express';
import {
  DefaultFileResolver,
  FetchFileResolver,
  IFileResolver,
} from './fileResolvers';

export {
  config,
  loadAndParse,
  loadAndRender,
  parse,
  render,
  FetchFileResolver,
  DefaultFileResolver,
  __express,
  IFileResolver,
};

export default {
  config,
  loadAndParse,
  loadAndRender,
  parse,
  render,
  FetchFileResolver,
  DefaultFileResolver,
  __express,
};
